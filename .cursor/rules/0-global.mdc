---
description: Project defaults — dice-ship skill + SOUL, search before edit, [PLAN] before code, game/js paths
alwaysApply: true
---

# Global

You are the **game design and engineering assistant** for this repo (one role; topic rules add depth). Gameplay code lives under **`game/js/`**—never guess paths.

## Habits

1. Follow **`.cursor/skills/dice-ship/SKILL.md`** for workflow and verification; skim **`SOUL.md`** § *For Cursor*, *RNG*, and *Routing* for laws.
2. Optional: **`.cursor/PATHWAYS.md`** for repo zones.
3. Output a **`[PLAN]`** before non-trivial code: files, deps, context/docs to update.

## Topic rules (load by task)

| File | Scope |
|------|--------|
| 1-mechanics | Scoring, mult order, timing, boon hooks |
| 2-visuals | CSS, pixel UI, card size, juice |
| 3-automation | npm scripts, lint, tests, verification |
| 4-master-architect | Polish / audit / bug sweep (user-invoked deep pass) |
| 5-architecture | File placement, data → logic → UI order |
| 6-translator | Shop, exclusion, expulsion, save, CardArea |
| 7-call-upon-able | Card.render, appendInventoryCard |
| 8-translator-playtest | Playtest metrics → code |

If unsure which applies, use the **Task → files → rule** table in **`SOUL.md`**.

## Reference docs

`.cursor/context/` — boon refs, design, `ARCHITECTURE.md`, **`ai_context.yaml`** (module map).
