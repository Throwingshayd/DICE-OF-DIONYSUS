---
description: Gatekeeper — SOUL first, cursor rules primary. Search before edit, [PLAN] before build.
alwaysApply: true
---

# Global Context

## First Read: SOUL.md

**Read `SOUL.md` first.** It defines identity, rule table, read order, decision tree. Rules implement that structure.

## Rule Map

| Rule | Identity |
|------|----------|
| 0-global | This file — gatekeeper |
| 1-mechanics | The Judge — hand validation, mult order, timing |
| 2-visuals | The Artist — pixel art, card sizing, juice |
| 3-automation | The Engineer — dev/lint/test/playtest |
| 4-master-architect | The Architect — polish, bug-hunt (@ invoked) |
| 5-architecture | The Surveyor — file hierarchy, dependency order |
| 6-translator | The Translator — Lua→JS, exclusion, expulsion |
| 7-call-upon-able | The Reuser — Card.render(), appendInventoryCard() |
| 8-translator-playtest | The Playtester — playtest→translator bridge |

## Research First

Never guess file paths. Search before editing. `js/` = game logic. `.cursor/context/` = agent docs.

## Plan Before Build

Output a `[PLAN]` block before code. Include files, dependencies, meta to update.
